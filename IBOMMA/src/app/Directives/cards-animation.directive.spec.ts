import { CardsAnimationDirective } from './cards-animation.directive';

describe('CardsAnimationDirective', () => {
  it('should create an instance', () => {
    const directive = new CardsAnimationDirective();
    expect(directive).toBeTruthy();
  });
});
